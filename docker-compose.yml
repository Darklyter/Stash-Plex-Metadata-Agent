version: '3.8'

services:
  stashplexagent:
    build: .
    ports:
      - "7979:7979"
    volumes:
      - ./stashplexagent.config:/app/stashplexagent.config:ro
    restart: unless-stopped
    networks:
      - stash-network

networks:
  stash-network:
    driver: bridge